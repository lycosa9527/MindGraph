# MindGraph - AI-Powered Diagram Generation Platform
# Version: 5.117.31 (see VERSION file for current version)
# Python 3.8+ Required (3.13+ Recommended for Best Async Performance)
#
# INSTALLATION INSTRUCTIONS:
#   1. conda create -n mindgraph python=3.13 -y && conda activate mindgraph
#   2. pip install -r requirements.txt
#   3. playwright install chromium  # Install Playwright browser binaries
#   4. Copy .env.example to .env and configure settings
#   5. DingTalk MindBot QR account bind: sudo apt-get install -y libzbar0
#      (Ubuntu 24.04+ may resolve libzbar0 → libzbar0t64; either is fine)
#
# PRODUCTION DEPLOYMENT NOTES:
#   - Review and configure all environment variables in .env
#   - Set feature flags (FEATURE_MINDMATE, FEATURE_KITTY_AGENT, etc.)
#   - Configure database URL for your environment
#   - Set DEBUG=False for production
#   - Optional: Configure Sentry DSN for error tracking
#   - Image I/O: Pillow for qrcode generation, OCR, avatars
#
# LAST UPDATED: 2026-08-05 - PyPI floor sweep (datasets 5; websockets 15.x for langgraph-sdk; pydantic <3; qdrant 1.18.x)

# ============================================================================
# WEB FRAMEWORK
# ============================================================================
fastapi>=0.141.1
uvicorn[standard]>=0.52.1
jinja2>=3.1.6
# Note: Starlette is managed by FastAPI - do not pin explicitly
pydantic>=2.13.4,<3.0  # Pin <3 until v3 is released; models use ConfigDict (v3-ready)
pydantic-settings>=2.14.2
email-validator>=2.3.0  # Pydantic email validation
geoip2>=5.3.0  # GeoLite2 Country MMDB (overseas email registration)
orjson>=3.11.9  # Fast JSON serialization for API responses

# ============================================================================
# HTTP & NETWORKING
# ============================================================================
aiohttp>=3.14.3  # Used by Dify client (MindMate integration)
openai>=2.53.0
python-multipart>=0.0.32
httpx[http2]>=0.28.1  # Primary async HTTP client with HTTP/2 (LLM clients, SMS)
alipay-sdk-python>=3.7.1160  # Alipay Open Platform (gateway; e.g. alipay.trade.page.pay)
mcp[cli]>=2,<3  # MCP Streamable HTTP at /api/mcp (MCPServer; transport kwargs on streamable_http_app)
# Cap <16: langgraph-sdk 0.4.x (pulled by langgraph≥1.2.10) requires websockets>=14,<16.
# Bump to 17+ only after langgraph-sdk raises that ceiling.
websockets>=15.0.1,<16  # WebSocket support (Omni realtime voice)
dingtalk-stream>=0.24.3  # DingTalk Stream SDK (background WebSocket for AI card callbackType=STREAM in groups)

# ============================================================================
# AI & LANGUAGE PROCESSING
# ============================================================================
# LangChain 1.x - Major upgrade with create_agent, LangGraph runtime, and middleware
langchain>=1.3.14
langchain-community>=0.4.2
langchain-core>=1.5.3
langchain-openai>=1.4.1
langchain-text-splitters>=1.1.2  # Text chunking for RAG (Personal Knowledge Space)
langgraph>=1.2.10
langgraph-checkpoint>=4.1.1
dashscope>=1.26.5
opencc-python-reimplemented>=0.1.7  # Simplified vs Traditional detection for thinking-mode language policy

# ============================================================================
# CONFIGURATION & ENVIRONMENT
# ============================================================================
PyYAML>=6.0.3
python-dotenv>=1.2.2

# ============================================================================
# ASYNC SUPPORT
# ============================================================================
nest_asyncio>=1.6.0
aiofiles>=25.1.0
anyio>=4.14.2

# ============================================================================
# BROWSER AUTOMATION & IMAGE PROCESSING
# ============================================================================
playwright>=1.62.0
Pillow>=12.3.0  # Image I/O; qrcode generation, OCR, avatars
PyMuPDF>=1.28.0  # PDF rendering for library cover extraction (fitz module)
qrcode[pil]>=8.2  # Bind QR generation on web (requires Pillow)
pyzbar>=0.1.9  # Optional QR decode utilities — requires system libzbar0: apt install libzbar0

# ============================================================================
# SYSTEM MONITORING & UTILITIES
# ============================================================================
psutil>=7.2.2  # System resource monitoring
watchfiles>=1.2.0  # File watching for auto-reload
rich>=15.0.0  # Rich terminal output with progress bars (for migration progress)

# ============================================================================
# DATABASE & AUTHENTICATION
# ============================================================================
SQLAlchemy>=2.0.51
alembic>=1.19.0  # Database migrations
psycopg[binary]>=3.3.4  # PostgreSQL driver (psycopg3, async-compatible, works with PG18 async I/O)
python-jose[cryptography]>=3.5.0
bcrypt>=5.0.0  # Password hashing (direct bcrypt, passlib removed in v4.12.0)
captcha>=0.7.1
pycryptodome>=3.23.0  # AES decryption for bayi mode
# Note: Tencent SMS uses native async httpx calls (no SDK needed)

# ============================================================================
# PERSONAL KNOWLEDGE SPACE (RAG) DEPENDENCIES - REQUIRED
# ============================================================================
numpy>=2.5.1  # Numerical operations for embedding normalization and validation
qdrant-client>=1.18.0,<1.19  # Match server 1.18.x; upgrade server via scripts/setup/update_qdrant_server.py
jieba3>=1.0.2  # Chinese text segmentation for FTS5 full-text search (modern Python 3 rewrite, no pkg_resources)
langdetect>=1.0.9  # Language detection for documents
semchunk>=4.1.1  # Semantic text chunking (fast, token-aware, Chinese support)
tiktoken>=0.13.0  # OpenAI tokenizer for semchunk (accurate token counting)
spacy>=3.8.14  # NLP library for sentence-based chunking (offline, requires: python -m spacy download en_core_web_sm)
chonkie>=1.7.0  # Modern chunking library with multiple strategies (offline, base features)
datasets>=5.0.1  # Hugging Face datasets for RAG chunk-test benchmarks
pypdf>=6.14.2  # PDF text extraction (successor to PyPDF2; fallback: pdfplumber>=0.11.9)
python-docx>=1.2.0  # DOCX text extraction
python-pptx>=1.0.2  # PPTX text extraction
openpyxl>=3.1.5  # XLSX text extraction
beautifulsoup4>=4.15.0  # HTML text extraction
pdfplumber>=0.11.10  # PDF text extraction with page info (fallback for pypdf)
pytesseract>=0.3.13  # OCR for image text extraction (requires Tesseract binary)
celery>=5.6.3  # Background task processing for document uploads (REQUIRED - application will exit if not available)
# Note: Tesseract OCR binary must be installed separately:
#   - Windows: Download from https://github.com/UB-Mannheim/tesseract/wiki
#   - macOS: brew install tesseract
#   - Linux: apt-get install tesseract-ocr tesseract-ocr-chi-sim (for Chinese support)
# Note: Qdrant server is REQUIRED. On Linux: conda activate mindgraph &&
#       sudo -E env PATH="$PATH" "$(which python)" scripts/setup/setup.py (docs/QDRANT_SETUP.md)
#       Then set QDRANT_HOST=localhost:6333 in .env
# Note: Celery is REQUIRED and requires Redis as broker (already included above).
#       Application will exit if Celery cannot start or Qdrant is not available.

# ============================================================================
# CACHING (Redis - REQUIRED)
# ============================================================================
# redis (redis-py): latest stable on PyPI; bump floor when refreshing this file.
redis>=8.1.0
# XADD idmpauto= (Redis 8.6+ token stream); 8.4+ client features; caching, sessions.
# MindGraph refuses to start on Redis 8.6+ if asyncio xadd has no idmpauto=.
# Requires Python 3.10+ (Python 3.13+ recommended). For performance: pip install "redis[hiredis]"

# ============================================================================
# IP GEOLOCATION (Local Database - No External APIs)
# ============================================================================
py-ip2region>=3.0.4  # Local IP geolocation database (xdb format, loads into memory)
# Note: Run python scripts/setup/dashboard_install.py to fetch xdb files and dashboard assets
#       (see scripts/setup/setup_ip2region.md)

# ============================================================================
# CLOUD STORAGE (Optional)
# ============================================================================
cos-python-sdk-v5>=1.9.44  # Tencent Cloud Object Storage (COS) SDK for online backups

# ============================================================================
# PRODUCTION DEPLOYMENT (Optional but Recommended)
# ============================================================================
# gunicorn>=24.0.0  # WSGI server (not needed - uvicorn is async)
# python-json-logger>=3.2.1  # Structured JSON logging
# sentry-sdk[fastapi]>=2.20.0  # Error tracking - configure SENTRY_DSN in .env
# prometheus-fastapi-instrumentator>=7.0.0  # Prometheus metrics

# ============================================================================
# FRONTEND I18N HELPERS (optional local scripts under frontend/scripts/)
# ============================================================================
deep-translator>=1.11.4  # batch_translate_canvas_google.py (Google web API via deep_translator)

# ============================================================================
# TESTING (Development/CI)
# ============================================================================
pytest>=9.1.1
pytest-asyncio>=1.4.0
pytest-cov>=7.1.0  # Code coverage
pylint>=4.0.6  # Static analysis and PEP8 linting
basedpyright>=1.39.9  # Strict static typing (pyproject.toml [tool.basedpyright])
ruff>=0.16.1  # Fast formatter + linter (ruff format / ruff check); aligns with frontend ESLint+Prettier workflow
