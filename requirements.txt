# MindGraph - AI-Powered Graph Generation Application
# Version: 2.5.4
# Requirements for Python 3.8+ (Tested with Python 3.13.5)
# 
# Core Features:
# - AI-powered graph generation with Qwen and DeepSeek LLMs
# - Interactive D3.js visualization  
# - PNG export functionality
# - Comprehensive dependency validation
# - Professional startup sequence
# - Cross-platform compatibility
# - Clean classification system with no hardcoded fallbacks
#
# INSTALLATION INSTRUCTIONS:
# 1. Install Python packages:
#    pip install -r requirements.txt
#
# 2. Install Playwright browsers with system dependencies:
#    playwright install chromium --with-deps
#    
#    OR use the automated setup script:
#    python setup.py

# ============================================================================
# WEB FRAMEWORK - MIGRATION IN PROGRESS
# ============================================================================
# Flask (Current - will be removed after FastAPI migration complete)
Flask>=3.1.1
Werkzeug>=3.1.0
flask-cors>=6.0.0

# FastAPI (New - target framework)
fastapi>=0.104.0

# ============================================================================
# SERVER - MIGRATION IN PROGRESS
# ============================================================================
# Waitress (Current WSGI - will be removed after migration)
waitress>=3.0.0

# Uvicorn (New ASGI - target server, works on Windows + Ubuntu)
uvicorn[standard]>=0.24.0

# ============================================================================
# HTTP AND NETWORKING (Required for Production)
# ============================================================================
# requests (Current - will be removed after 100% async migration)
requests>=2.31.0

# aiohttp (Async HTTP client - target for 100% async)
aiohttp>=3.12.0

# python-multipart (For FastAPI form data and file uploads)
python-multipart>=0.0.6

# ============================================================================
# AI AND LANGUAGE PROCESSING (Required for Production)
# ============================================================================
langchain>=0.3.27
langchain-community>=0.3.27
langchain-core>=0.3.72

# ============================================================================
# CONFIGURATION AND ENVIRONMENT (Required for Production)
# ============================================================================
PyYAML>=6.0.1
python-dotenv>=1.0.1

# ============================================================================
# ASYNC AND CONCURRENCY (Required for Production)
# ============================================================================
nest_asyncio>=1.6.0

# ============================================================================
# BROWSER AUTOMATION (Required for Production - PNG generation)
# ============================================================================
# Note: After pip install, run: playwright install chromium --with-deps
# This installs the Chromium browser and required system dependencies
playwright>=1.54.0

# ============================================================================
# IMAGE PROCESSING (Required for Production - PNG export)
# ============================================================================
Pillow>=11.3.0

# ============================================================================
# SYSTEM MONITORING (Required for Production - status endpoints)
# ============================================================================
psutil>=6.0.0

# ============================================================================
# DEPENDENCY SUMMARY
# ============================================================================
# 
# REQUIRED THIRD-PARTY PACKAGES (16):
# - Flask, Werkzeug, flask-cors (Web framework)
# - waitress (Production WSGI server)
# - langchain, langchain-community, langchain-core (AI/LLM)
# - requests, aiohttp (HTTP clients)
# - playwright, Pillow (Browser automation & image processing)
# - python-dotenv (Environment configuration)
# - PyYAML (Configuration parsing)
# - nest_asyncio (Async utilities)
# - psutil (System monitoring)
#
# REMOVED UNUSED PACKAGES (v2.5.4):
# - pyee, asgiref, typing-extensions (not used in codebase)
# - pydantic, structlog, cryptography (not used in codebase)
#
# PYTHON STANDARD LIBRARY USED:
# - Core: os, sys, json, re, time, datetime, math, logging, pathlib
# - Async: asyncio, threading, concurrent.futures
# - Data: collections, typing, dataclasses, enum
# - Utils: functools, subprocess, tempfile, base64, colorsys
# - Others: socket, webbrowser, shutil, atexit
#
# ============================================================================
# INSTALLATION NOTES
# ============================================================================
# 
# QUICK START:
# 1. pip install -r requirements.txt
# 2. playwright install chromium --with-deps
# 3. Copy env.example to .env and configure API keys
# 4. python run_server.py
#
# AUTOMATED SETUP:
# python setup.py
# (Handles all dependencies including Playwright browsers)
#
# COMPATIBILITY:
# - Python 3.8+ (Tested with Python 3.13.5)
# - Node.js 18.19+ for D3.js dependencies
# - All packages use >= for minimum version compatibility
#
# ANALYSIS LAST UPDATED: 2025-10-01 (Version 2.5.4)