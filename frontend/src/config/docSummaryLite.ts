/**
 * Document Summary lite UI — extract-only flow without RAG corpus chrome.
 * Set false when full Knowledge Space / RAG UI returns.
 */
export const DOC_SUMMARY_LITE_UI = true

/** Hide sidebar Knowledge Space + RAG chunk-test entry while lite mode is on. */
export const HIDE_KNOWLEDGE_SPACE_NAV = DOC_SUMMARY_LITE_UI
